# Go2 Local Model Asset

The approved local model is:

```text
assets/models/go2/go2_mobile.glb
```

The GLB is bundled as a local Flutter asset and contains the `Idle` and `Walk`
animation clips. Robot mode changes switch clips on the existing viewer; they
do not reload the model or use a network URL.
